--- a/theme-editor.py	2026-04-25 16:26:46.048084860 +0100
+++ a/theme-editor.py	2026-04-25 18:35:30.077084064 +0100
@@ -32,6 +32,7 @@
 import subprocess
 import sys
 import time
+from pathlib import Path
 
 try:
     import tkinter
@@ -82,7 +83,10 @@
 # Resize editor if display is too big (e.g. 8.8" displays are 1920x480), can be changed later by zoom buttons
 RESIZE_FACTOR = 2 if (display.lcd.get_width() > 1000 or display.lcd.get_height() > 1000) else 1
 
-ERROR_IN_THEME = Image.open("res/docs/error-in-theme.png")
+# Get the directory where this script is located (to handle relative paths correctly)
+MAIN_DIRECTORY = str(Path(__file__).parent.resolve()) + "/"
+
+ERROR_IN_THEME = Image.open(MAIN_DIRECTORY + "res/docs/error-in-theme.png")
 
 
 def refresh_theme():
